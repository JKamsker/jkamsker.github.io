[build]
  command = "npm ci && bundle exec jekyll build && node scripts/optimize_css.mjs build --benchmark scripts/css_benchmark.json"
  publish = "build"

[build.environment]
  JEKYLL_ENV = "production"
